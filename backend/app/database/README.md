# Database

Database configuration, engine creation, and session helpers belong here.

The project starts with SQLite and is structured so SQLAlchemy can later switch from SQLite to PostgreSQL with configuration changes rather than route-level rewrites.
