"""TODO(Phase future): send transactional email when a prize is confirmed.

This module is a stub for MVP. Do not call real SMTP or third-party APIs here yet.
Planned integration points:
- After ``PrizeNotification`` is created or marked ready for email.
- Use a background worker or managed email provider (e.g. SES, SendGrid).

Example placeholder:
    def send_prize_email_winner(player_email: str, message: str) -> None:
        raise NotImplementedError
"""


def send_prize_email_placeholder(player_email: str, message: str) -> None:
    """Reserved for future email delivery — not implemented."""
    _ = (player_email, message)
